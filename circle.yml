dependencies:
  pre:
    - gem install bundler --pre 

deployment:
  production:
    branch: master
    commands: 
      - heroku run rake db:migrate cache:clear --app tiny-training-ci-app
    heroku:
      appname: tiny-training-ci-app